const podium = ['Joao', 'Maria', 'Jose']

podium.forEach(function(nome, indice){   // ForEach: para cada
    console.log(`${indice +1} - ${nome}`)
})

// ou seja, para cada indice+1 e nome encontrado, retorna no array
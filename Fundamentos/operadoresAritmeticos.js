const [a, b, c, d] = [10, 20, 30, 40]
const [e, f, g, h] = [50, 60, 70, 80]


let adicao = d + c
let subtracao = h - b
let multiplicacao = a * b
let divisao = h / b
let modulo = a % 2   // % = modulo, significa que a / 2 resta:

console.log(adicao, subtracao, multiplicacao, divisao, modulo)